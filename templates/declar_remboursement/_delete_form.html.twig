<form method="post" action="{{ path('declar_remboursement_delete', {'id': declar_remboursement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ declar_remboursement.id) }}">
    <button class="btn">Delete</button>
</form>
