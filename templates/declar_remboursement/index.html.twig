{# {% extends 'Templateuseri.html.twig' %}

{% block title %}DeclarRemboursement index{% endblock %}

{% block body %}
    <h1>DeclarRemboursement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Montant</th>
                <th>Date</th>
                <th>Periode</th>
                <th>Type</th>
                <th>Justificatif</th>
                <th>Status</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for declar_remboursement in declar_remboursements %}
            <tr>
                <td>{{ declar_remboursement.id }}</td>
                <td>{{ declar_remboursement.montant }}</td>
                <td>{{ declar_remboursement.date ? declar_remboursement.date|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ declar_remboursement.periode }}</td>
                <td>{{ declar_remboursement.type }}</td>
                <td>{{ declar_remboursement.justificatif }}</td>
                <td>{{ declar_remboursement.status }}</td>
                <td>
                    <a href="{{ path('declar_remboursement_show', {'id': declar_remboursement.id}) }}">show</a>
                    <a href="{{ path('declar_remboursement_edit', {'id': declar_remboursement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


{% endblock %}
 #}

  <!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no" />
		{# <link rel="stylesheet" href="assets/css/main.css" />
		<link rel="stylesheet" href="assets/css/main.css" /> #}
        
		<link rel="stylesheet" href="{{asset('/css/main.css')}}" type="text/css"/>
        <link rel="stylesheet" href="{{asset('/css/bootstrap.min.css')}}" type="text/css"/>
	</head>
	<body class="is-preload">

		<!-- Header -->
        <header id="header">

            <!-- Logo -->
                <div class="logo">
                    <a href="">Sutura</a><span>Maroc</span>
                </div>

            <!-- Nav -->
                <nav id="nav">
                    <ul>
                        <li><a href="{{ path('indexadmin') }}">Administration</a></li>
                        <li><a href="{{ path('app_logout') }}">Se déconnecter</a></li>
                    </ul>
                </nav>

        </header>
            <div class=" w-100" style="position:absolute; top:100px;">
                <h1>Liste des remboursements</h1>
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Etudiant</th>
                            <th>Montant</th>
                            <th>Date</th>
                            <th>Periode</th>
{#                             <th>Type</th>
 #}                            <th>Justificatif</th>
                            <th>Status</th>
                            <th>actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for declar_remboursement in declar_remboursements %}
                            <tr>
                                <td>{{ declar_remboursement.id }}</td>
                                <td>{{ declar_remboursement.Etudiant.nom ~ ' ' ~ declar_remboursement.Etudiant.prenom}}</td>
                                <td>{{ declar_remboursement.montant }}</td>
                                <td>{{ declar_remboursement.date ? declar_remboursement.date|date('Y-m-d H:i:s') : '' }}</td>
                                <td>{{ declar_remboursement.periode }}</td>
{#                                 <td>{{ declar_remboursement.type }}</td>
 #}                                <td>{{ declar_remboursement.justificatif }}</td>
                                <td>{{ declar_remboursement.status }}</td>
                                <td>
                                    <a class="btn btn-secondary btn-sm" href="{{ path('declar_remboursement_edit', {'id': declar_remboursement.id}) }}">Traiter</a>
                                    <a class="btn btn-danger btn-sm" href="{{ path('declar_remboursement_delete', {'id': declar_remboursement.id}) }}">Supprimer</a>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="8">no records found</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
    </body>
</html>