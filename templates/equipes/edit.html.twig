{% extends 'base.html.twig' %}

{% block title %}Edit Equipes{% endblock %}

{% block body %}
    <h1>Edit Equipes</h1>

    {{ include('equipes/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('equipes_index') }}">back to list</a>

    {{ include('equipes/_delete_form.html.twig') }}
{% endblock %}
